// GoogleLogin.tsx — now a thin re-export wrapper over CloudSyncCard
// The full logic (auth, sync status, export, etc.) lives in CloudSyncCard.tsx
export { CloudSyncCard as GoogleLogin } from './CloudSyncCard';

import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'app.netlify.tasbeehdikr.native',
  appName: 'Tasbeehly',
  webDir: 'dist'
};

export default config;
